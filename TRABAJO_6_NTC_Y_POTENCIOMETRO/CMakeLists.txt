# CMakeLists.txt principal del proyecto ESP-IDF.
# Este archivo va en la carpeta raíz del proyecto.
cmake_minimum_required(VERSION 3.16)

# Incluye las herramientas de construcción de ESP-IDF.
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Nombre del proyecto.
project(TRABAJO_6_NTC_Y_POTENCIOMETRO)
